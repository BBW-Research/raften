#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -P -- "$(dirname -- "$0")/.." && pwd -P)

TOP_LEVEL=$(git -C "$ROOT" rev-parse --show-toplevel 2>/dev/null || true)
if [ "$TOP_LEVEL" != "$ROOT" ]; then
  git -C "$ROOT" init -q
fi

exec "$ROOT/scripts/validate"
