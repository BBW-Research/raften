#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -P -- "$(dirname -- "$0")/.." && pwd -P)
unset PYTHONHOME
PYTHON=${PYTHON:-python3}
PYTHONPATH="$ROOT/src:$ROOT" exec "$PYTHON" -P -m release_tools.benchmark "$@"
