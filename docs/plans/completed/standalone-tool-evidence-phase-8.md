# Standalone tool Phase 8 qualification evidence

- Status: Phase 8 completed on 2026-09-02. The MIT-licensed artifacts, pilots, supported-platform qualification, hosted CI, ownership, and default-branch controls satisfy every release-blocking acceptance item.
- Candidate identity: release tooling exported and built the exact clean commit `c67b394601fcaedad6cb69c852f0f4bb940788ec`. The public distribution name is `repo-context-policy`, while the import package and CLI remain `repo_context` and `repo-context`. Version 1.0.0 supports CPython 3.12 and 3.13 on macOS and Linux; Windows is intentionally unqualified.
- Build boundary: `scripts/build-release` rejects dirty, non-repository, or changed source state, exports regular Git blob bytes from `HEAD`, verifies and privately stages the exact hash-locked wheelhouse, builds the sdist and wheel under PEP 517 isolation without indexes, creates the zipapp, verifies archive membership and metadata, and writes an exact sorted checksum manifest. The first committed-state build exposed that outer `pip --isolated` ignored environment-only index controls; commit `615fc5d` adds explicit `--no-index` and `--find-links` arguments, and the read-only reviewer found no remaining package-resolution leak.
- Locked build tools: `build==1.5.0` at SHA-256 `13f3eecb844759ab66efec90ca17639bbf14dc06cb2fdf37a9010322d9c50a6f`, `packaging==26.3` at `d7193f7c8e4e93f444fde0262bf90af30e16fa0ad0ad44cb553c87339b23cd1c`, `pyproject-hooks==1.2.0` at `9e5c6bfa8dcc30091c74b0cf803c81fdd29d94f01992a7707bc97babb1141913`, and `setuptools==84.0.0` at `51a52592b3b99e102b609654876bd65f19f999935166d1352678931132b0c670`. Runtime dependencies remain Python's standard library only.
- Candidate artifacts: `repo_context_policy-1.0.0-py3-none-any.whl` is SHA-256 `dd541fbe8c4f4f2bbd802dfc62d7ce020d7c1e277dcf1f873122b39bab6b29e0`; `repo_context_policy-1.0.0.tar.gz` is `c669117aa1b79a2099c6c5edcc79a7de2a8a9203b7e69d3552b079fff9e9e508`; and `repo-context-1.0.0.pyz` is `0926b899a9ff91a39e98162362979c62ecb5dffc522ec7f6951f499b2f1f21a3`. The wheel metadata declares `License-Expression: MIT`, both legal files, and the canonical BBW-Research URLs; the sdist and zipapp also contain `LICENSE` and `NOTICE`.
- Determinism: independent builds from the same commit and commit epoch under Python 3.12.14 and 3.13.13 produced byte-identical zipapps at the recorded hash. The contract does not claim byte-identical backend-generated wheels or source archives.
- macOS qualification: on macOS 26.6.2 arm64 with Git 2.55.0, the artifact-era 512-test suite passed under Python 3.12.14 and 3.13.13 with warnings treated as errors; one non-UTF-8-filename capability test skipped on each interpreter. Both interpreters separately verified checksums and archive structure, installed the wheel and sdist into fresh environments without dependency or index access, exercised the installed command, and executed the zipapp. After adding the structural CODEOWNERS assertion, the complete local suite contains 513 tests and remains green.
- Linux qualification: pinned Debian Bookworm images `python:3.12.14-bookworm@sha256:581429e3df12d76e6af4be5ab7d0e7fc2013eb57dc23d2de691411c8efdbb970` and `python:3.13.14-bookworm@sha256:8b9a8b28d9cc221c6ab5d40e9cfcd99429959f6a8f5171612a99147975ab043f` ran as Linux arm64 with Git 2.39.5, image pulls and networking disabled, a read-only root filesystem, a bounded temporary filesystem, all capabilities dropped, and `no-new-privileges`. Each image passed all 512 tests without skips and independently qualified all three artifacts. Git 2.39.5 is therefore the conservative supported floor for version 1.
- Consumption and migration: [release documentation](../../release/index.md) specifies installed-package, pinned-zipapp, and vendored-source workflows; [migration documentation](../../migration/index.md) replaces bootstrap JSON assumptions with explicit v1 TOML classifications and exact monotonic first-adoption debt.
- Pilots: isolated clones of [the private pilot](../../pilots/index.md), NanoDLLM, and Research Vault completed with zero blocking diagnostics at their recorded commits. Their separate policies and proposed indexes are frozen as fixtures; exact generated debt identities are recorded by hash because the commit-specific sidecars are not checked in. Pilot findings produced two generic changes only: exact generated-sidecar classification in the starter and literal-component matcher optimization. No engine path contains project-specific behavior. The 100,000-path synthetic baseline completed a check in 3.583 seconds at 353.9 MiB peak RSS; complete JSON audit output was 88,120,435 bytes.
- Repository controls: `https://github.com/BBW-Research/repo-context` exists as a private repository with `main` as its default branch, and authenticated organization member `@taiqihe` has administrator access. GitHub reports no CODEOWNERS parsing error. Hosted [workflow run 33629864145](https://github.com/BBW-Research/repo-context/actions/runs/33629864145) passed offline documentation validation, all four native Ubuntu/macOS and Python 3.12/3.13 qualification jobs, and the stable `Required repository policy` aggregator. Protected `main` requires that check with strict branch freshness, pull requests, one approval, dismissal of stale approvals, approval of the most recent reviewable push, code-owner review, resolved conversations, and administrator enforcement; force pushes and deletions are disabled. A user-approved `@taiqihe` bypass existed only for the final completion push and was removed after its required workflow passed.
- License decision: `LICENSE` grants the MIT License with `Copyright (c) 2026 BBW-Research`; `NOTICE` separately preserves the project-owned bootstrap MIT terms. Package metadata declares the SPDX expression and both legal files, and all three qualified artifact formats contain both files.
- Review: the read-only reviewer approved the release/pilot implementation and separate policy baseline `a009463`, the offline-build correction, the MIT licensing baseline, and CODEOWNERS coverage. The two new policy classifications remain narrow, scanned, and subject to unchanged byte limits. The final review found no other issue and made completion conditional only on a provider readback proving empty bypass lists and preservation of the recorded protection fields after the final workflow passed.
- Remaining non-blocking work: repository visibility is intentionally private for now. No release tag, package-index publication, or trusted publisher is configured because publication was not requested; any future publication must select, rebuild, and requalify an exact release commit rather than reusing this Phase 8 candidate after source-facing documentation changes. Four existing files remain above advisory thresholds but below their hard ceilings, and one non-UTF-8-filename capability assertion may skip on macOS.
