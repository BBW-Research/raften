# Standalone tool Phase 7 completion evidence

- Completed: 2026-09-02.
- Self-hosting: `scripts/context-check` exports only the repository `src` directory and launches `python3 -P -m repo_context check --repo "$ROOT"`, so normal local, bootstrap, and CI policy checks select the standalone engine without installation and ignore ambient `PYTHONPATH` or a hostile package in the caller's working directory. `scripts/validate` compiles production and test code but no longer treats the frozen oracle as a production module. CI retains full history and the existing event-derived `BASE_REF` behavior.
- Oracle boundary: `tools/check_repository_policy.py` remains byte-identical to the accepted scene-maker source at SHA-256 `bb796f2304f3692089a5e87cef55a7fbe3e7c4c9521d6d2299b738a84f7ebd11`. Normal entrypoints do not select it, production recursively rejects imports from `tools` and `tests` plus frozen-checker string references, and execution is confined to source-characterization and paired-compatibility tests. The adapted clean JSON policy moved from the root bootstrap path to `tests/fixtures/seed/clean-policy.json`; its SHA-256 is recorded and enforced through the source manifest.
- Compatibility: eight paired command-line fixtures cover four equivalent outcomes and four intended improvements: clean success, hard limits, missing entrypoints, limit weakening, intermediate documentation ancestors, missing local targets, unavailable requested bases, and unsafe config paths. The complete classification is recorded in `docs/reference/scene-maker-compatibility.md`; no comparison defect remains.
- Adopted policy: the generic initializer template remains package-owned while this repository's policy explicitly classifies the three frozen scene-maker files as scanned `vendored` content and `tests/fixtures/**` as scanned `fixture` content at unchanged 20,480/25,600-byte limits. The required authored-to-non-authored transition produced exactly two `RAT008` findings against Phase 6 and no other ratchet finding; the reviewer approved it as the separate baseline commit `6816154`. Subsequent comparison against that reviewed baseline reports zero changes.
- Self-audit: in the clean Phase 7 state, the standalone engine governs 153 Git-visible paths as 148 authored, two fixture, and three vendored files; 34 Markdown documents are reachable across nine documentation directories; the three-member `bootstrap` context totals 4,327 bytes; and the HEAD comparison reports no blocking diagnostic or ratchet change. Four existing files are above advisory warning thresholds but remain below their hard ceilings.
- Archive bootstrap: `scripts/bootstrap` resolves its physical root and initializes a repository whenever the exact archive directory is not already the Git top level, including when unpacked below an unrelated parent worktree. The archive regression copies the complete source tree to a path containing spaces, removes Git history, blocks network proxies, injects a hostile ambient `PYTHONPATH`, and runs the entire validation suite successfully offline without recursive test expansion.
- Tests: 19 tests added since Phase 6 cover paired compatibility, root self-hosting, explicit special-file classification, production/oracle separation, fixture provenance, hostile caller state, normal entrypoint selection, and a freshly unpacked archive. The complete suite contains 477 tests with one host-filesystem capability skip.
- Validation: `scripts/validate` and `BASE_REF=HEAD scripts/validate` pass on Python 3.14.7. Full warnings-as-errors suites pass on Python 3.12.14 and 3.13.13 with 477 tests and one capability skip each. Shell syntax, `git diff --check`, the frozen-oracle hash/diff check, and offline Lychee validation with 90 successful links, two excluded links, and zero errors also pass.
- Review: the read-only reviewer approved the explicit policy-baseline transition and independently verified the classification counts, unchanged scan limits, recursive production boundary, provenance hash, hostile caller test, archive bootstrap, full supported-version suites, and clean committed-clone behavior. No finding remains open; CI itself was not executed during the local review.
- Remaining work: Phase 8 owns package and release metadata, reproducible distribution artifacts, macOS and Linux installation qualification, consumption and migration documentation, external pilot repositories, and eventual remote repository controls.
- Risks: current full-suite and archive evidence is macOS arm64. Linux execution and installed-artifact qualification remain Phase 8 work. Windows is intentionally outside the supported scope under [decision 0005](../../decisions/0005-supported-platforms.md). One non-UTF-8-filename assertion skips when the host filesystem cannot create that byte sequence.
