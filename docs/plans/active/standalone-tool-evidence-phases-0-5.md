# Standalone tool phases 0–5 evidence

This companion to the [active standalone-tool plan](standalone-tool.md) retains completed phase evidence so the executable checklist remains compact. Evidence moves here only after its phase gate is complete.

## Phase 0 completion evidence

- Completed: 2026-09-01.
- Baseline: root commit `6b4263d` records the validated 50-file starter before implementation changes.
- Provenance: tests verify the recorded scene-maker repository and commit, every copied file's verbatim mode and Git blob, and the checker SHA-256. The frozen checker remains byte-identical at `bb796f2304f3692089a5e87cef55a7fbe3e7c4c9521d6d2299b738a84f7ebd11`.
- Characterization: 74 tests cover the complete source surface and deterministic CLI streams. Real temporary repositories isolate ambient Git configuration, use fixed identities and timestamps, commit reproducible base states, mutate tracked and untracked content, and run both seed and target entrypoints. Structured assertions were sufficient, so Phase 0 added no golden output file.
- Intended differences: `docs/reference/scene-maker-seed.md` maps the known intended differences to retained seed evidence and an owning target phase.
- Packaging: accepted decision 0004 pins `setuptools==84.0.0` after a 2026-09-01 review of release age, Python compatibility, ownership, maintenance, license, dependency footprint, execution surface, and artifact provenance. Runtime dependencies remain empty and no environment-manager lock is warranted yet.
- Validation: `scripts/validate` and the offline Lychee check pass without network access. The seed policy check, compile checks, CLI help, diff check, and all 74 tests pass locally on Python 3.12.14 and 3.13.13 without package installation. The suite also passes with hostile ambient `GIT_DIR` and injected Git configuration.
- Review: the read-only Phase 0 reviewer found and verified fixes for Git-environment isolation, Windows-invalid fixture names, overly broad symlink skips, deterministic commit evidence, and dependency-review completeness. No review finding remains open.
- Remaining work: Phase 1 begins immutable models and strict TOML parsing; no production seed behavior was extracted during Phase 0.
- Risks: local cross-version evidence is macOS arm64; Linux and Windows execution remains dependent on the supported-version CI and release matrix. Symlink assertions skip only when the host cannot create symlinks. Isolated distribution builds will require the reviewed backend artifact to be available and will receive explicit offline artifact verification in Phase 8.

## Phase 1 completion evidence

- Completed: 2026-09-01.
- Models: frozen, slotted records and immutable tuple collections cover the complete policy surface plus inventory entries, source locations, links, diagnostics, and run results. Exact and pattern selectors are distinct union variants, preventing dual-selector parser states.
- Configuration: the UTF-8 `tomllib` boundary validates exact table shapes, types, canonical paths, repository-pattern syntax, ordered file rules, the mandatory final scanned authored catch-all, deterministic override specificity, documentation dependencies, context sets, ratchets, and governed intentional exceptions. Configuration failures use stable `CFG001` through `CFG013` and `EXC001` identities with unambiguous field paths and deterministic source/position/code ordering.
- Serialization: `render_starter_policy()` returns a package-owned byte template that is LF-normalized, final-newline-terminated, byte-identical to the normative root policy, and model-equivalent after parsing. No general TOML writer or runtime dependency was added.
- Contract: `docs/specs/configuration-v1.md` is the authoritative exact schema for table shapes, static precedence, path and pattern syntax, exceptions, and diagnostic identities. The product and architecture documents link to it rather than duplicating the detailed contract.
- Tests: 70 Phase 1 tests cover every immutable record family, complete root-policy projection, optional record defaults, template round trips, strict shape/type failures, every semantic invariant, malformed path and pattern cases, ambiguity proofs, exception governance, side-effect isolation, and diagnostic identity/order. The full 144-test repository suite passes on Python 3.12.14 and 3.13.13, including warnings-as-errors probes.
- Validation: `scripts/validate`, the transitional policy check with `--base-ref HEAD`, hostile ambient Git-variable execution, and offline Lychee validation pass. The frozen checker remains byte-identical at `bb796f2304f3692089a5e87cef55a7fbe3e7c4c9521d6d2299b738a84f7ebd11`.
- Review: the read-only reviewer found and verified fixes for wildcard-aligned disjointness, literal stars inside character classes, public diagnostic sort order, Unicode C1 controls, and exact schema wording. The final review reports no findings and confirms config/model code has no CLI, reporting, Git, scanning, or matcher-bypass dependency.
- Remaining work: Phase 2 owns repository-root validation, Git inventory, base blobs, canonical platform path normalization, and actual glob matching. Phase 1 intentionally implements only pattern syntax and conservative static ambiguity analysis.
- Risks: static ambiguity analysis intentionally rejects equal-specificity overrides unless disjointness is provable from aligned literals. Exception expiry against an evaluation date remains a deterministic run-time check; parsing validates date types and chronology without consulting the wall clock.

## Phase 2 completion evidence

- Completed: 2026-09-01.
- Inventory: explicit canonical roots work independently of package and process location. Three strictly framed NUL-delimited Git views produce globally path-sorted immutable records for tracked, non-ignored untracked, deleted, sparse-missing, regular, executable, symlink, other, and gitlink states. SHA-1 and SHA-256 object identities, unresolved index stages, filesystem races, parent symlinks, and Windows junction shapes have explicit behavior and stable `GIT001` through `GIT009` diagnostics.
- Process boundary: Git runs through a canonical absolute executable resolved before the child changes directory. The sanitized search path rejects empty, relative, selected-repository, directory-symlink, and executable-symlink routes into repository content and is inherited by Git with Windows current-directory search disabled. Argument vectors, binary streams, prompt and lock suppression, configuration isolation, null-device hooks and filesystem monitor, disabled lazy fetch, disabled replacement objects, and a timeout keep operations deterministic and noninteractive.
- Base access: safe ref resolution, sorted recursive tree metadata, binary-search lookup, and lazy exact-object blob reads preserve invalid UTF-8 and NUL bytes without checkout or index/worktree mutation. Gitlinks remain commit objects rather than readable blobs, and local replacement refs cannot substitute validated identities.
- Matcher: canonical platform conversion and an iterative dynamic-programming engine define full-path `*`, `?`, component-only `**`, character classes, dotfiles, literal candidate metacharacters, exact case, and unnormalized Unicode behavior without `fnmatch`, regular-expression backtracking, or filesystem globbing.
- Contract: `docs/specs/repository-paths-and-globs-v1.md` is authoritative for canonical paths and matcher semantics. `docs/architecture/inventory.md` owns root, process, snapshot, base-object, race, and diagnostic behavior; the product, configuration, and architecture indexes link to those contracts rather than duplicating them.
- Tests: 57 tests added since Phase 1 cover real repositories, hostile environments, process non-execution, malformed Git bytes, races, sparse checkout, gitlinks, symlinks, SHA-256 when supported, base-state non-mutation, path normalization, the complete matcher matrix, public-facade preservation, and architecture boundaries. The full 201-test suite passes on Python 3.12.14 and 3.13.13 with warnings as errors; one macOS filesystem-capability test for an invalid UTF-8 filename skips. The reviewer independently compared 949,221 matcher cases with complete agreement.
- Validation: `scripts/validate`, the transitional policy check with `--base-ref HEAD`, hostile ambient Git-variable execution, target CLI help, `git diff --check`, and offline Lychee validation pass. The frozen checker remains byte-identical at `bb796f2304f3692089a5e87cef55a7fbe3e7c4c9521d6d2299b738a84f7ebd11`.
- Review: the read-only reviewer found and verified fixes for executable filesystem-monitor configuration, replacement refs, readlink error identity, sparse-checkout absence, nested Windows drive components, empty and NUL roots, base-state mutation evidence, mixed-source failure ordering, and repository-controlled Git executable search. The final implementation review and post-split architecture review report no remaining findings.
- Remaining work: Phase 3 owns classification, shared no-follow content reads, byte accounting, file budgets, context sets, and explain data. Public CLI integration remains Phase 6 work.
- Risks: local cross-version evidence is macOS arm64 with Git 2.55.0; real Linux and Windows execution remains dependent on CI, including Windows junction and executable-search behavior. The supported Git-version floor remains deferred to distribution validation. The inventory facade is now split from record decoding, worktree inspection, and shared errors; Phase 3 must extend those boundaries without recreating a god module. Centralized no-follow content opening must reduce the documented parent-inspection-to-leaf-open race.

## Phase 3 completion evidence

- Completed: 2026-09-01.
- Policy resolution: `size_policy.py` compiles every rule, override, exception, and context pattern once. Resolution records first-rule and first-pattern provenance, exact-over-pattern and most-specific-pattern override precedence, monotonic ordinary limits, exact-over-pattern intentional exceptions, injected-date expiry, and distinct ordinary versus effective scan and threshold state.
- Evaluation: `sizes.py` classifies NUL and strict UTF-8 states, measures exact raw bytes, emits only the strongest advisory or blocking threshold diagnostic, retains requested plaintext for later parsing without another read, reports unclassified and ineffective policies, computes declaration-ordered context sets with path-sorted de-duplicated membership, distinguishes every missing exact-member state, and exposes immutable audit and explain records.
- Repository reads: regular inventory entries carry mode, device, inode, size, modification time, and change time. The public current-content reader uses descriptor-relative no-follow and nonblocking opens, bounds reads to snapshot size plus one byte, verifies descriptor and path identity before and after reading, distinguishes observed path changes as `GIT005`, and fails closed as `GIT009` when secure primitives are unavailable.
- Audit: largest governed files sort by descending raw size then path and retain effective policy, including explicit authored, generated, vendored, fixture, legal, and intentional-exception state. Classification counts and explain resolution are deterministic for both inventoried and nonexistent canonical paths.
- Diagnostics: run-time configuration identities `CFG014` and `CFG015`, budget identities `CTX001` through `CTX005`, and injected-date expiry identity `EXC002` have stable severities, locations or field paths, and structured details. Equal-specificity overlapping exception patterns now receive the existing static ambiguity identity `CFG011`.
- Contract: `docs/specs/file-budgets-v1.md` is authoritative for classification, exceptions, content reads, thresholds, context accounting, audit, explain, and diagnostic semantics. Architecture and configuration documents link to it. Completed phase evidence moved into this companion log so the executable plan remains below its policy ceiling without losing history.
- Tests: 42 Phase 3 tests added since Phase 2 cover pure precedence, all content states, ASCII/multibyte/CRLF boundaries, extension neutrality, exact one-read reuse, structured diagnostics, every context-member state, aggregate accounting, audit/explain ordering, mixed tracked and untracked repository behavior, snapshot identity, same-size and in-place mutation, symlink and intermediate redirection, FIFO replacement, bounded concurrent growth, post-open replacement, unusual open errors, and fail-closed behavior. The full 243-test suite has one capability skip for a host filesystem that cannot create an invalid UTF-8 filename.
- Validation: `scripts/validate`, warnings-as-errors suites on Python 3.12 and 3.13, the transitional policy check with `--base-ref HEAD`, hostile ambient Git configuration, `git diff --check`, and offline Lychee validation pass. Direct evaluation of the repository's 96 Git-visible files reports one advisory `CTX001` for an existing test file, no blocking diagnostic, and a within-limit bootstrap context. The frozen checker remains byte-identical at `bb796f2304f3692089a5e87cef55a7fbe3e7c4c9521d6d2299b738a84f7ebd11`.
- Review: the read-only reviewer identified and verified fixes for unbounded concurrent growth, blocking FIFO replacement, a path-following fallback, and type-change misclassification after unusual open errors. The final review reports no findings and confirms the Phase 3 gate is satisfied.
- Remaining work: Phase 4 consumes retained plaintext to implement Markdown extraction and the documentation graph without reopening files. Public runner and renderer integration remains Phase 6 work.
- Risks: local evidence remains macOS arm64, so Linux validation is still required before the Phase 8 supported-platform gate. Platforms without descriptor-relative no-follow opens currently fail closed with `GIT009`; Windows is outside the supported-platform scope under [decision 0005](../../decisions/0005-supported-platforms.md). Evaluation dates are explicit API inputs; Phase 6 must preserve that determinism while wiring commands.

## Phase 4 completion evidence

- Completed: 2026-09-01.
- Contract: `docs/specs/markdown-graph-v1.md` is authoritative for the bounded Markdown subset, physical line model, ordered local-destination normalization, heading and explicit-anchor semantics, documentation discovery, hierarchy, target checks, reachability, and `DOC001` through `DOC011`. Configuration now rejects authored-document symlink following because version 1 has no safe alias semantics.
- Extraction: pure standard-library modules implement LF, CRLF, and CR line handling; stateful fence, code-span, and comment masking; inline, image, full, collapsed, and shortcut reference occurrences; strict percent and UTF-8 decoding; source-markup decoding; external-link separation; repository-safe dot-component resolution; headings, explicit IDs, supported inline HTML, and deterministic collision suffixes. Reference labels use the CommonMark 999-source-character bound, and duplicate-heading and nested-bracket work is bounded rather than quadratic.
- Graph: immutable, path-sorted records discover every current lowercase `.md` below each root, preserve intermediate empty ancestors, distinguish images and local assets from navigation, enforce directory indexes and direct sibling and child-index routes, traverse deterministically from multiple roots, check exact fragments, and keep configured entrypoints outside the root set. Evaluation consumes only one inventory snapshot and retained text; it performs no filesystem, Git, subprocess, or network access.
- Retention: every present regular lowercase `.md` plus configured non-Markdown entrypoints is selected before shared budget evaluation. Effectively unscanned requested text is classified and decoded from the same single verified read without acquiring byte-limit state or diagnostics. Governed symlinks are never requested as text or followed.
- Source differences: integration fixtures prove the target rejects the seed's intermediate-directory gap and detects local target and fragment failures the seed parser misses. The frozen seed checker remains byte-identical at `bb796f2304f3692089a5e87cef55a7fbe3e7c4c9521d6d2299b738a84f7ebd11`.
- Tests: 62 tests added since Phase 3 cover parser state and ignored contexts, source positions and all physical line endings, destination transform and safety order, external-link boundaries, nested image and navigation contexts, bounded deep nesting, references, Unicode, headings and HTML IDs, every graph flag and diagnostic family, hierarchy gaps, exclusions, multiple roots, cycles, orphans, assets, symlinks, shared one-read retention, 100,000-path selection, a 1,000-directory hierarchy, immutable records, architecture boundaries, seed differences, and repository self-hosting. The full suite contains 305 tests with one host-filesystem capability skip.
- Validation: `scripts/validate` and the transitional policy check with `--base-ref HEAD` pass on Python 3.14.7. Full warnings-as-errors suites pass on Python 3.12.14 and 3.13.13 with 305 tests and one capability skip each. `git diff --check`, the offline Lychee check, and the frozen-oracle hash check pass. Direct self-graph evaluation covers 108 Git-visible entries, 32 retained text paths, 28 governed Markdown nodes, 9 hierarchy directories, and 52 navigation edges with zero diagnostics.
- Review: the read-only Phase 4 reviewer found and verified fixes for transformation precedence, remote-link classification, nested occurrence contexts, fake HTML anchors, multiline-code anchors, unresolved reference display, physical line consistency, duplicate-heading and bracket-nesting complexity, reference-label bounds, image-alt routing, escaped comments, repository-root directory normalization, and supported non-tag inline HTML. The final review reports no remaining high, medium, or low findings and confirms the behavioral gate is satisfied.
- Remaining work: Phase 5 owns file and policy ratchets. Phase 6 must connect the completed size and documentation evaluators to the public CLI, reports, audit, explain, and init flows without reopening retained files or changing diagnostic identities.
- Risks: version 1 is a deliberately bounded, documented Markdown subset rather than a complete CommonMark implementation. Every Git-visible lowercase Markdown file is retained even outside configured roots so linked fragment targets can be checked; future scale work must preserve bounded parser behavior. Authored-document symlink aliases remain unsupported until a repository-contained identity and fragment contract exists.

## Phase 5 completion evidence

- Completed: 2026-09-02.
- Contract and model: `docs/specs/ratchets-v1.md` is authoritative for current-candidate eligibility, Git and first-adoption baselines, diagnostic reconciliation, unavailable states, manifest bytes, the conservative policy partial order, and stable `RAT001` through `RAT013` identities. Immutable records distinguish baseline source, migration state, observed base size and identity, and a real migration ceiling; ineligible baselines never advertise a ceiling.
- File ratchet: pure evaluation considers only present regular authored plaintext above the current ordinary hard limit, ignores intentional-exception relief, and compares exact paths against the immediate base blob. New paths, ordinary-sized bases, ineligible types or content, and growth fail; equal size and partial reduction remain debt; returning within the ordinary limit clears debt without a base read. Only proven debt suppresses the ordinary `CTX002`, preserving the raw assessment for audit and explain.
- Git behavior: recursive base-tree metadata now includes directories so directory-to-file replacement is a deterministic type change. Regular and executable blobs share one eligible type; symlinks, gitlinks, trees, non-blobs, NUL data, and invalid UTF-8 do not. Rename behavior is exact delete-plus-add, and real repository tests verify comparison leaves HEAD, index bytes, staged state, and worktree bytes unchanged.
- First adoption: a strict versioned JSON sidecar captures only exact oversized-candidate paths, positive raw sizes, and SHA-256 content identities. Parsing rejects duplicate or unknown keys, unsafe paths, selector-shaped fields, invalid numbers and identities, and duplicate entries. Literal Git filename metacharacters and JSON-escaped characters retain exact path identity. Once a base policy exists, Git takes precedence over the sidecar.
- Policy ratchet: split comparison modules enforce the base-controlled partial order for file-rule identity and ordering, finite limits, overrides, unscanned coverage, documentation roots and graph flags, entrypoints, context sets, ratchet switches, and governed exceptions. Conservative overlap checks prevent selector rewrites, rule reordering, nested reachability seeds, or fallback exposure from silently weakening policy. Size evaluation and policy comparison share one authoritative fail-closed `CFG015` predicate.
- Determinism: absent and all-zero base refs are distinct nonblocking unavailable states; nonzero resolution, shallow-history, tree, blob, and type failures retain operational Git identities. Base-policy absence and first-adoption precedence are specified for Phase 6 orchestration. All collections and diagnostics are explicitly sorted, and pure comparators perform no filesystem, subprocess, environment, or clock access.
- Tests: 62 tests added since Phase 4 cover the full file truth table, partial-reduction ceilings, diagnostic reconciliation, manifest round trips and malformed input, every policy domain, fail-closed exception transitions, exact filename semantics, rename and type changes, real Git blob comparison, and non-mutation. The full suite contains 367 tests with one host-filesystem capability skip.
- Validation: `scripts/validate`, warnings-as-errors suites on Python 3.12 and 3.13, the transitional policy check with `--base-ref HEAD`, `git diff --check`, offline Lychee validation, and the frozen-oracle hash check pass. The frozen checker remains byte-identical at `bb796f2304f3692089a5e87cef55a7fbe3e7c4c9521d6d2299b738a84f7ebd11`.
- Review: the read-only reviewer verified fixes for rule-order and guard interactions, unscanned coverage, nested documentation roots, exception fallback exposure, fail-closed `CFG015` transitions, exact manifest filename semantics, tree metadata, and invalid migration ceilings. The shared effective-policy predicate removes drift between current-state and policy-ratchet enforcement. No review finding remains open.
- Remaining work: Phase 6 owns runner selection among Git, manifest, absent-ref, and operational-error outcomes; public output; `audit` and `explain`; and guarded `init --capture-debt` writes. Phase 7 owns normal-workflow self-hosting and retirement of the frozen transitional checker.
- Risks: policy-pattern removal and overlap remain deliberately conservative where general glob containment is not proven, so an intentional tightening may still require owner review. Local execution evidence is macOS arm64; Linux remains a Phase 8 release-matrix requirement, while Windows is outside the supported-platform scope under [decision 0005](../../decisions/0005-supported-platforms.md).
